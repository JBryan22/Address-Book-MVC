@{
  Layout = "_Layout";
}

<h2 class="headline">Your Contacts</h2>
<h4>Click On A Contact To View Details</h4>

@if (@Model.Count == 0)
{
  <p>You currently have no contacts. Click below to add a new contact to your addres book!</p>
}

<ul>
  @foreach (var contact in @Model)
  {
    <li><a href="contact/@contact.GetId()">@contact.GetName()</a></li>
    <form action="/contact/remove/@contact.GetId()" method="post">
      <button class="btn btn-danger btn-sm" type="submit">Remove This Contact</button>
    </form>
  }
</ul>

@if (@Model.Count != 0)
{
  <form action="/contact/clear" method="post">
    <button class="btn btn-danger btn-sm" type="submit">Clear All Contacts</button>
  </form>
}

<p><a href="/contact/create" class="btn btn-success btn-sm">Add A New Contact</a></p>
